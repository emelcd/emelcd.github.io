# AGENTS.md

Instructions for AI coding agents working in this repository.

## Project

Personal portfolio for Miguel López (`emelcd`): **https://emelcd.github.io**

- This repo = **frontend only** (GitHub Pages).
- Content source of truth = backend repo `/home/malopez/emelcd.backend` → `data/cv.json`.
- Public API: `https://emelcdbackend.vercel.app/api/cv`.

Respond to the user in **Spanish** unless they ask otherwise.

## Stack

- Bun · Vite 8 · React 19 · TypeScript
- Tailwind CSS v4 (`@tailwindcss/vite`) · shadcn-style UI on `@base-ui/react`
- oxlint · path alias `@/` → `src/`

```bash
bun install
bun run dev
bun run fetch-content
bun run build
bun run lint
```

## Content rules (critical)

1. **Never edit site copy in this repo.** Edit `data/cv.json` in the backend.
2. `src/lib/content-data.json` is a **build-time fallback** regenerated by
   `bun run fetch-content`. Local edits get overwritten.
3. **No hardcoded UI copy** in components — read from `usePreferences().t`
   (localized `Content`). Exception: decorative terminal `out` strings.
4. Content loads twice by design: baked JSON first, then runtime `fetchContent()`
   so backend edits go live without a frontend rebuild.

## Architecture map

| Area | Where |
|------|--------|
| Types, accents, fonts, fetch | `src/lib/content.ts` |
| Lang / theme / accent / font state + localStorage | `src/context/preferences.tsx` |
| Preferences popover UI | `src/components/ThemeMenu.tsx` |
| Sections | `src/components/sections/*` composed in `src/App.tsx` |
| Terminal commands | `src/lib/terminal.ts` + `TerminalCard.tsx` |
| Admin CV editor | `src/components/admin/AdminPage.tsx` (route `/admin`) |

## Theming

Visitor prefs (persisted in `localStorage` key `emelcd-prefs`):

- language (`es` / `en`)
- dark / light
- accent (`ACCENTS` / `ACCENT_ORDER` in `content.ts`)
- font (`FONTS` / `FONT_ORDER` in `content.ts`)

CSS wiring:

- Accent → `--accent-400/600/800` on `<html>`
- Font → **`--font-ui`** on `<html>` (not `--font-sans` directly)
- Tailwind `@theme` maps `--font-sans` / `--font-heading` → `var(--font-ui)`
- FOUC prevention: inline script in `index.html` applies stored prefs before paint

**Do not** set `--font-sans` alone to change fonts — `@theme inline` inlines
Geist unless you go through `--font-ui`.

Navbar exposes one preferences control (`ThemeMenu` / `SlidersHorizontal`).
Keep language, theme, color, and font **inside that single menu**.

## UI conventions

- Functional components, `@/` imports, Tailwind utilities, base-ui primitives.
- Match existing visual language; don’t invent a parallel design system.
- Prefer extending `ThemeMenu` / `preferences.tsx` over new settings panels.

## Deploy

- Push to `main` → `.github/workflows/static.yml` → GitHub Pages.
- Does **not** deploy to Vercel. Backend deploys separately.
- No secrets in this repo; `/api/cv` is public (`CORS *`).

## Git

- Commit only when the user asks.
- Conventional Commits (`feat` / `fix` / `refactor` / …).
- Don’t amend pushed commits; don’t force-push `main`.
