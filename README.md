# emelcd.github.io

Portfolio site built with Vite + React + TypeScript and [shadcn/ui](https://ui.shadcn.com).

## Development

```bash
bun install
bun run dev
```

## Build

```bash
bun run build
```

Deploys automatically to GitHub Pages on push to `main` via `.github/workflows/static.yml`.
